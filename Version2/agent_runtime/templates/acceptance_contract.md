# Acceptance Contract

Use measurable ACs only.

## AC-01: <name>
Given:
When:
Then:

Oracle:
- Observable output/state:
- Pass threshold:
- Evidence location:

## AC-02: <name>
Given:
When:
Then:

Oracle:
- Observable output/state:
- Pass threshold:
- Evidence location:

## Notes
- Avoid circular ACs (for example: "if unblocked then continue").
- State who owns each transition and the condition to transition.
- Add failure-path ACs for reliability and data integrity.
